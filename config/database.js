const Sequelize = require('sequelize');

const config = {
    username: "root",
    password: "fuckyou13",
    database: "economizemais_teste",
    host: "localhost",
    dialect: "mysql"
}

module.exports = config